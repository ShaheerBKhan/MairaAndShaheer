# frontend

This is a Vite + React + TypeScript starter scaffold for MairaAndShaheer.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Build for production:

```bash
npm run build
npm run preview
```
